@using UINProject.Models;
@model List<Estudiantes>

@{
    ViewBag.Title = "AsignarEstudiante";
}

@*<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <h1 class="text-center text-primary">Listado de Estudiantes Disponibles</h1>
        </div>
        <br />
        <table class="table-striped table-bordered">
            <thead>
                <tr>
                    <th>Nombre: </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count() = 0)
                {
                    foreach (var es in Model)
                    {
                        <tr>
                            <td>@es.NombreEstudiante</td>
                            <td>
                                <button class=" btn btn-info" onclick="Asignar('@es.IdEstudiante','@es.NombreEstudiante')">
                                    <i class="glyphicon glyphicon-check"></i> Asignar
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="2" class="text-center">
                            <b>No hay registros disponibles!!</b>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>*@